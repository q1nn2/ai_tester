# Чек-лист: Авторизация

- [ ] **AUTH-UI-001 – Успешный логин с валидными данными** (priority: high) [tags: ui, smoke, auth]  
  - Пользователь вводит корректный email и пароль, попадает на страницу профиля `/profile` без ошибок.

- [ ] **AUTH-API-001 – Успешный логин по API** (priority: high) [tags: api, smoke, auth]  
  - Запрос `POST /auth/login` с валидными данными возвращает статус 200 и токен в ответе.

